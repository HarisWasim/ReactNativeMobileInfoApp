import React from 'react';
import Entypo from 'react-native-vector-icons/Entypo';
import { Platform, PixelRatio} from 'react-native';


const navIconSize = (_DEV_ === false && Platform.OS === 'android') ? PixelRatio.getPixelSizeForLayoutSize(25) : 25;

const replaceSuffixPattern

const icons = {
    home: [25, Entypo]
}